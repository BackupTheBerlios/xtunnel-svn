bin_PROGRAMS = xtunnel
xtunnel_SOURCES = xtunnel.c xtserver.c xtclient.c

# set the include path found by configure
INCLUDES= $(all_includes)

# the library search path.
xtunnel_LDFLAGS = $(all_libraries) 
